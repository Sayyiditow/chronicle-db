port=9099
dbPath=.data
dbArchPath=.data-arch/
env=dev
primary=true

# standby dbs, comma separated
replication=false
queueSize=512
standbyDbUrls=
standbyDbPorts=

# Prometheus metrics endpoint. Set metricsPort=0 to disable.
# Localhost is always allowed; add scraper IPs to metricsAllowedIps (comma-separated).
metricsPort=9100
metricsAllowedIps=